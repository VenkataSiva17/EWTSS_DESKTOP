<Window x:Class="EWTSS_DESKTOP.Presentation.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login"
        Height="300"
        Width="400"
        WindowStartupLocation="CenterScreen">

    <Grid Background="#F4F6F9">
        <Border Background="White"
                Width="300"
                Height="200"
                CornerRadius="10"
                Padding="20"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

            <StackPanel>

                <TextBlock Text="EWTSS Login"
                           FontSize="20"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Margin="0 0 0 20"/>

                <TextBox
                    Margin="0 0 0 10"
                    Height="30"
                    Text="{Binding Username}" />

                <PasswordBox
                    Height="30"
                    Margin="0 0 0 15"/>

                <Button
                    Content="Login"
                    Height="35"
                    Background="#007ACC"
                    Foreground="White"
                    Command="{Binding LoginCommand}"/>

            </StackPanel>

        </Border>
    </Grid>
</Window>